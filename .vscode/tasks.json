{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/实验2.cpp",
                "-o",
                "${workspaceFolder}/实验2"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}